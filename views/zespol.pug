html
    head
        title Informacje o zespole
    body
        a(href='/') Ekran startowy
        br
        a(href='/wydzial?wydzial='+zespol[2]) Pokaż informacje o wydziale
        h3 Informacje o zespole
        p Nazwa: #{zespol[1]}
        button(onclick="document.getElementById('zn').hidden=!document.getElementById('zn').hidden") Pokaż/ukryj formularz zmiany nazwy
        - let h=true
        - let potencjalnaNazwa=zespol[1]
        if (blad && blad === "nk")
            - h=false
            - potencjalnaNazwa=nk
        form(action = "/zmien/zespol", method = "POST" id="zn" hidden=h)
                div
                    p Nowa nazwa:
                    input(name = "nazwa" value = potencjalnaNazwa required)
                    if (blad && blad === "nk")
                        p(color="red") Istnieje już zespół o takiej nazwie na tym wydziale
                    input(name = "z" value = zespol[0] hidden)
                br
                button(type = "submit") Zmień nazwe
        p='Wydział: '
            a(href='/wydzial?wydzial='+zespol[2]) #{zespol[4]}
        button(onclick="document.getElementById('zw').hidden=!document.getElementById('zw').hidden") Pokaż/ukryj formularz zmiany wydziału
        - let h2=true
        if (blad && blad === "wk")
            - h2=false
        form(action = "/zmien/zespol", method = "POST" id="zw" hidden=h2)
                div
                    p Nowy wydział:
                    select(name = "wydzial")
                        each row in wydzialy
                            option(value=row[0]) #{row[1]}
                    if (blad && blad === "wk")
                        p(color="red") Istnieje już zespół o takiej samej nazwie na wybranym wydziale
                    input(name = "z" value = zespol[0] hidden)
                br
                button(type = "submit") Zmień wydział
        h4 Pracownicy w zespole:
            table(border='2')
                each row in pracownicy.rows
                    tr
                        td
                            a(href='/pracownik?pracownik=' + row[0]) #{row[1]} #{row[4]} #{row[5]}
        button(onclick="document.getElementById('dodajStudenta').hidden=!document.getElementById('dodajStudenta').hidden") Pokaż/ukryj formularz dodawania pracownika
        - let h4=true
        if (blad && blad === "pesel")
            - h4=!true
        form(action = "/dodaj/pracownik", method = "POST" id="dodajStudenta" hidden=h4)
            div
                label Imię:
                input(name = "imie" value = imie required)
                br
                label Nazwisko:
                input(name = "nazwisko" value = nazwisko required)
                br
                label PESEL (11 cyfr):
                input(type="text" name = "pesel"  pattern="[0-9]{11}" oninvalid="this.setCustomValidity('PESEL powinien składać się z 11 cyrf')" value=pesel required)
                if (blad && blad === "pesel")
                    p(color="red") Istnieje już osoba o takim PESELU
                br
                label Stopień naukowy:
                input(name = "tytul" value = tytul required)
                input(name = "zespol" value = zespol[0] hidden)
            button(type = "submit") Dodaj pracownika
        br
        p
        br
        form(action = "/usun/zespol", method = "POST" onsubmit="return confirm('Na pewno chcesz usunąć ten Zespół?');")
            input(name = "zespol" value = zespol[0] hidden)
            input(name = "wydzial" value = zespol[2] hidden)
            button(type = "submit" color="red") Usuń Zespół
